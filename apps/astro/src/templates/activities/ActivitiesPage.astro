---
import Layout from '@/src/layouts/Layout.astro'
import { sanityFetchWithCdn } from '@/utils/sanity.fetch'
import metadataFetch from '@/utils/metadata.fetch'
import Breadcrumbs from '@/src/layouts/Breadcrumbs.astro'
import { PortableTextQuery, type PortableTextValue } from '@/src/components/ui/portable-text'
import Components, { Components_Query, type ComponentsProps } from '@/src/components/Components.astro'
import { getLangFromPath, type Language } from '@/global/languages'
import Listing, {
  DEFAULT_PARTICIPANT_GROUPS,
  Listing_Query,
  type ListingProps,
  type ParticipantFilterGroup,
} from '@/src/components/activites/category/Listing.astro'
import { NoSearchResults_Query, type NoSearchResultsProps } from '@/src/components/activites/NoSearchResults.astro'
import { parseFilters, getOrderClause } from '@/utils/filters'
import ContactForm from '@/src/components/global/ContactForm/index.astro'
import type { FormStateTypes } from '@/src/global/types'
import { ImageDataQuery, type ImageDataProps } from '@/src/components/ui/image'

type FormProps = {
  formHeading?: PortableTextValue
  formParagraph?: PortableTextValue
  formVisualImage?: ImageDataProps
  overrideFormState?: boolean
  formState?: FormStateTypes
}

type Props = NonNullable<Awaited<ReturnType<typeof fetchData>>> & {
  currentPage: number
  currentCategory: string | null
  canonicalUrl?: string
  doNotIndex?: boolean
}

export async function fetchData(
  lang: Language,
  category: string | null,
  _pageNumber: number,
  searchParams?: URLSearchParams
) {
  const filters = parseFilters(searchParams || new URLSearchParams())
  const orderClause = getOrderClause(filters.order)

  const globalFilters = await sanityFetchWithCdn<{
    listingFilters?: {
      activityParticipantGroups?: ParticipantFilterGroup[]
    }
  }>({
    query: `
      *[_type == "global" && language == $language][0] {
        listingFilters {
          activityParticipantGroups[]{
            label,
            min,
            max
          }
        }
      }
    `,
    params: { language: lang },
    tag: 'page.activities-list.filters',
  })

  const participantGroups = (() => {
    const configured = (globalFilters?.listingFilters?.activityParticipantGroups || [])
      .filter((group) => typeof group?.label === 'string' && typeof group?.min === 'number')
      .map((group) => ({
        label: group.label,
        min: group.min,
        ...(typeof group.max === 'number' ? { max: group.max } : {}),
      }))
      .slice(0, 6)
      .filter((group) => typeof group.max !== 'number' || group.max >= group.min)

    return configured.length >= 2 ? configured : DEFAULT_PARTICIPANT_GROUPS
  })()

  // Build the full category slug to match exactly (with trailing slash)
  const categorySlug = lang === 'pl' ? `/pl/integracje/kategoria/${category}/` : `/en/activities/category/${category}/`

  const page = await sanityFetchWithCdn<
    {
      name: string
      slug: string
      globalFormDefaults?: {
        heading?: PortableTextValue
        paragraph?: PortableTextValue
      }
      selectedCategory: {
        name: string
        slug: string
        heading: PortableTextValue
        description: {
          long: string
        }
        overwriteActivityPageComponents?: boolean
        components?: ComponentsProps
        formOverrides?: {
          heading?: PortableTextValue
          paragraph?: PortableTextValue
          formVisualImage?: ImageDataProps
          overrideFormState?: boolean
          formState?: FormStateTypes
        }
      } & ListingProps
      components: ComponentsProps
      escapeHatchHeading?: string
      escapeHatchText?: string
    } & NoSearchResultsProps &
      FormProps
  >({
    query: `
      *[_type == "Activities_Page" && language == $language][0] {
        name,
        "slug": slug.current,
        "globalFormDefaults": *[_type == "global" && language == $language][0].inquiryFormDefaults {
          ${PortableTextQuery('heading')}
          ${PortableTextQuery('paragraph')}
        },
        ${PortableTextQuery('formHeading')}
        ${PortableTextQuery('formParagraph')}
        ${ImageDataQuery('formVisualImage')}
        escapeHatchHeading,
        escapeHatchText,
        overrideFormState,
        formState {
          success {
            ${PortableTextQuery('heading')}
            ${PortableTextQuery('paragraph')}
            highlightedSocialMedia[] -> {
              name,
              link,
              iconString,
            },
          },
          error {
            ${PortableTextQuery('heading')}
            ${PortableTextQuery('paragraph')}
          },
        },
        ${NoSearchResults_Query}
        "selectedCategory": *[_type == "ActivitiesCategory_Collection" && slug.current == $categorySlug][0] {
          name,
          "slug": slug.current,
          ${PortableTextQuery('heading')}
          description{
            long,
          },
          overwriteActivityPageComponents,
          formOverrides {
            ${PortableTextQuery('heading')}
            ${PortableTextQuery('paragraph')}
            ${ImageDataQuery('formVisualImage')}
            overrideFormState,
            formState {
              success {
                ${PortableTextQuery('heading')}
                ${PortableTextQuery('paragraph')}
                highlightedSocialMedia[] -> {
                  name,
                  link,
                  iconString,
                },
              },
              error {
                ${PortableTextQuery('heading')}
                ${PortableTextQuery('paragraph')}
              },
            },
          },
          ${Components_Query}
          ${Listing_Query(orderClause, participantGroups)}
        },
        ${Components_Query}
      }
    `,
    params: {
      language: lang,
      category,
      categorySlug,
      minParticipants: filters.minParticipants,
      maxParticipants: filters.maxParticipants,
      minPrice: filters.minPrice,
      maxPrice: filters.maxPrice,
      activityType: filters.activityType,
      duration: filters.duration,
      embeddingResults: null,
    },
    tag: 'page.activities-list',
  })

  if (!page || (category && !page.selectedCategory)) return null

  const metadata = await metadataFetch(page?.selectedCategory?.slug || page.slug)
  return { page, metadata, participantGroups }
}

export async function staticPathsCategory(lang: Language) {
  const categories = await sanityFetchWithCdn<{ lang: Language; slug: string }[]>({
    query: `
      *[_type == "ActivitiesCategory_Collection" && language == $language][] {
        "lang": language,
        "slug": slug.current,
      }
    `,
    params: { language: lang },
    tag: 'page.activities-list.categories',
  })

  return categories.map(({ slug }) => ({
    params: {
      category: slug.split('/').slice(-2).join('/'),
    },
  }))
}

const { page, metadata: _metadata, currentCategory, canonicalUrl, doNotIndex, participantGroups } = Astro.props

const lang = getLangFromPath(Astro.url.pathname) as Language

const metadata = {
  ..._metadata,
  title: _metadata.title,
  description: _metadata.description,
  openGraphImage: _metadata.openGraphImage,
  path: Astro.url.pathname,
  ...(canonicalUrl && { canonicalUrl }),
  ...(doNotIndex && { doNotIndex }),
}

const breadcrumbs = [
  { name: page.name, path: page.slug },
  ...(page?.selectedCategory ? [{ name: page.selectedCategory.name, path: page.selectedCategory.slug }] : []),
]

// Determine which components to use based on the category settings, filtering out page builder ContactForms
const componentsToRender = (() => {
  const categoryComponents = (page.selectedCategory?.components || []).filter((c) => c._type !== 'ContactForm')
  const activityPageComponents = (page.components || []).filter((c) => c._type !== 'ContactForm')

  // If category has no components, always use activity page components
  if (categoryComponents.length === 0) {
    return activityPageComponents
  }

  // If category has components and overwrite is explicitly enabled (true), use only category components
  if (page.selectedCategory?.overwriteActivityPageComponents === true) {
    return categoryComponents
  }

  // Default behavior: append category components after activity page components
  // This handles cases where overwriteActivityPageComponents is false, null, or undefined
  return [...activityPageComponents, ...categoryComponents]
})()

// Resolve form heading/paragraph: category override → singleton default
const formHeading = page.selectedCategory?.formOverrides?.heading || page.formHeading || page.globalFormDefaults?.heading
const formParagraph =
  page.selectedCategory?.formOverrides?.paragraph || page.formParagraph || page.globalFormDefaults?.paragraph
const formVisualImage = page.selectedCategory?.formOverrides?.formVisualImage || page.formVisualImage
const resolvedOverrideFormState = page.selectedCategory?.formOverrides?.overrideFormState ?? page.overrideFormState
const resolvedFormState = page.selectedCategory?.formOverrides?.overrideFormState
  ? page.selectedCategory?.formOverrides?.formState
  : page.formState
---

<Layout {...metadata}>
  <Breadcrumbs data={breadcrumbs} firstItemType="ActivitiesListing" />
  <Listing
    selectedCategory={page.selectedCategory.name}
    heading={page.selectedCategory.heading}
    description={page.selectedCategory.description.long}
    totalActivitiesByCategory={page.selectedCategory.totalActivitiesByCategory}
    listing={page.selectedCategory.listing}
    currentPage={1}
    currentCategory={currentCategory}
    participantRangeCounts={page.selectedCategory.participantRangeCounts}
    activityTypes={page.selectedCategory.activityTypes}
    durationCounts={page.selectedCategory.durationCounts}
    priceRange={page.selectedCategory.priceRange}
    noResults={page.noResults}
    participantGroups={participantGroups}
    escapeHintText={page.escapeHatchHeading}
    escapeHintCta={page.escapeHatchText}
  />
  {formHeading && (
    <ContactForm
      index={10}
      sectionId={lang === 'pl' ? 'kontakt' : 'contact'}
      heading={formHeading}
      paragraph={formParagraph || []}
      formVisualImage={formVisualImage}
      overrideFormState={resolvedOverrideFormState}
      state={resolvedFormState}
      variant="activity_listing"
      showInquiries={false}
      animate={false}
    />
  )}
  <Components data={page.selectedCategory.listing.length > 0 ? componentsToRender : page.noResults.components} hasPreviousSections />
</Layout>

<script>
  import { trackEvent } from '@/utils/track-event'

  // Track activities list view
  document.addEventListener('DOMContentLoaded', () => {
    // Get activity items from the page
    const activityItems = document.querySelectorAll('[data-activity-card]') as NodeListOf<HTMLElement>
    if (!activityItems.length) return

    // Get category information
    const categoryName = (document.querySelector('.Listing') as HTMLElement)?.dataset.categoryName || ''

    // Create array of items for tracking
    const items = Array.from(activityItems)
      .slice(0, 10)
      .map((activity, index) => {
        const activityName = activity.dataset.activityName || ''
        const activityId = activity.dataset.activityId || ''
        const pricing = Number(activity.dataset.pricing || '')

        return {
          item_name: activityName,
          item_id: activityId,
          item_price: Number.isFinite(pricing) ? pricing : undefined,
          price: Number.isFinite(pricing) ? pricing : undefined,
          index,
        }
      })

    // Track the event if we have items
    if (items.length > 0) {
      const contentIds = items.map((item) => item.item_id).filter(Boolean) as string[]
      trackEvent({
        ga4: {
          eventName: 'view_item_list',
          params: {
            items,
            item_list_name: categoryName,
          },
        },
        meta: {
          eventName: 'ViewCategory',
          contentName: categoryName,
          params: {
            category_name: categoryName,
            content_ids: contentIds.length ? contentIds : undefined,
          },
        },
      })
    }
  })
</script>
