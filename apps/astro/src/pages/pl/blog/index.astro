---
import BlogPage, { fetchData } from '@/src/templates/blog/BlogPage.astro'

const data = await fetchData('pl', null, 1)

if (!data) return Astro.rewrite('/pl/404')
---

<BlogPage {...data} currentPage={1} currentCategory={null} />
