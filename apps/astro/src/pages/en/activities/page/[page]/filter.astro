---
export const prerender = false

const searchParams = Astro.url.searchParams.toString()
const redirectUrl = searchParams ? `/en/activities/filter?${searchParams}` : '/en/activities/'

return Astro.redirect(redirectUrl, 301)
---
