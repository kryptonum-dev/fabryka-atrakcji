<div class="skeleton-loader">
  <header class="header">
    <div class="info-box skeleton"></div>
    <h1 class="cart-heading">
      <span class="text skeleton-text"></span>
      <span class="count skeleton"></span>
    </h1>
    <div class="inputs">
      <div class="input-field participants skeleton"></div>
      <div class="input-field date skeleton"></div>
    </div>
  </header>

  <div class="hotels-section-skeleton">
    <h2 class="cart-heading cart-heading-s">
      <span class="title skeleton-text"></span>
      <span class="count skeleton"></span>
    </h2>
    <ul class="list">
      <li class="hotel-item skeleton-item">
        <div class="main-content">
          <div class="img-wrapper skeleton"></div>
          <div class="content">
            <div class="stars skeleton"></div>
            <div class="name skeleton-text"></div>
            <ul class="details">
              <li class="skeleton-text"></li>
              <li class="skeleton-text"></li>
              <li class="skeleton-text"></li>
            </ul>
          </div>
          <div class="actions">
            <div class="addons skeleton"></div>
            <div class="remove skeleton"></div>
          </div>
        </div>
      </li>
    </ul>
  </div>

  <div class="activities-section-skeleton">
    <h2 class="cart-heading cart-heading-s">
      <span class="title skeleton-text"></span>
      <span class="count skeleton"></span>
    </h2>
    <ul class="list">
      <li class="activity-item skeleton-item">
        <div class="main-content">
          <div class="img-wrapper skeleton"></div>
          <div class="content">
            <div class="name skeleton-text"></div>
            <div class="participants skeleton-text"></div>
          </div>
          <div class="actions">
            <div class="addons skeleton"></div>
            <div class="remove skeleton"></div>
          </div>
        </div>
      </li>
    </ul>
  </div>

  <div class="actions">
    <div class="button-placeholder skeleton"></div>
    <p class="additional-info skeleton-text"></p>
  </div>
</div>

<style lang="scss">
  .skeleton {
    background: var(--neutral-300, #ede6de);
    position: relative;
    overflow: hidden;
    border-radius: 0.25rem;

    &::after {
      position: absolute;
      top: 0;
      right: 0;
      bottom: 0;
      left: 0;
      transform: translateX(-100%);
      background-image: linear-gradient(
        90deg,
        rgba(255, 255, 255, 0) 0,
        rgba(255, 255, 255, 0.2) 20%,
        rgba(255, 255, 255, 0.5) 60%,
        rgba(255, 255, 255, 0)
      );
      animation: shimmer 2s infinite;
      content: '';
    }

    @keyframes shimmer {
      100% {
        transform: translateX(100%);
      }
    }
  }

  .skeleton-text {
    height: 1rem;
    margin-bottom: 0.5rem;
    border-radius: 0.25rem;
    @extend .skeleton;
  }

  .skeleton-loader {
    display: flex;
    flex-direction: column;
    gap: clamp(2rem, calc(2vw / 0.48), 2.25rem);
    width: 100%;

    .header {
      .info-box {
        height: 4rem;
        margin-bottom: clamp(1rem, calc(1vw / 0.48), 1.5rem);
      }

      .cart-heading {
        .text {
          width: 10rem;
          height: 2rem;
          display: inline-block;
        }

        .count {
          width: 1.75rem;
          height: 1.75rem;
          border-radius: 50%;
          display: inline-block;
          margin-bottom: 0.625rem;
        }
      }

      .inputs {
        margin-top: clamp(1rem, calc(1vw / 0.48), 1.5rem);
        display: flex;
        gap: 1rem;

        .participants {
          max-width: 7.625rem;
          height: 3.5rem;
        }

        .date {
          flex: 1;
          height: 3.5rem;
        }
      }
    }

    .hotels-section-skeleton,
    .activities-section-skeleton {
      .cart-heading {
        margin-bottom: 1rem;

        .title {
          width: 8rem;
          height: 1.5rem;
          display: inline-block;
        }
      }

      .list {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
      }
    }

    .actions {
      .button-placeholder {
        width: 100%;
        height: 3rem;
        border-radius: 0.25rem;
        margin-bottom: 0.5rem;
      }

      .additional-info {
        width: 80%;
        height: 2rem;
        margin: 0 auto;
      }
    }

    // Skeleton item specific styles
    .skeleton-item {
      &.hotel-item,
      &.activity-item {
        .main-content {
          display: grid;
          grid-template-columns: auto 1fr auto;
          background-color: var(--neutral-100, #faf7f7);
          padding: 0.25rem;
        }

        .img-wrapper {
          height: 10rem;
        }

        .content {
          padding: 0.25rem 0;

          .stars {
            width: 5rem;
            height: 1rem;
            margin-top: 0.5rem;
          }

          .name {
            width: 12rem;
            height: 1.5rem;
            margin: 0.75rem 0;
          }

          .details li,
          .participants {
            width: 14rem;
            height: 1rem;
            margin-top: 0.375rem;
          }
        }

        .actions {
          display: flex;
          flex-direction: column;
          align-items: flex-end;
          padding: 0.5rem 0.25rem;

          .addons {
            width: 8rem;
            height: 2.75rem;
            border-radius: 0.25rem;
          }

          .remove {
            width: 2.75rem;
            height: 2.75rem;
            border-radius: 50%;
            margin-top: 0.5rem;
          }
        }
      }

      &.hotel-item .img-wrapper {
        max-width: 13rem;
      }

      &.activity-item .img-wrapper {
        max-width: 6.5rem;
      }
    }
  }

  // Media query for mobile adjustments
  @media (max-width: 47.9375rem) {
    .skeleton-loader {
      .skeleton-item {
        &.hotel-item,
        &.activity-item {
          .main-content {
            grid-template-columns: 1fr;
          }

          .img-wrapper {
            width: 100%;
            max-width: 100% !important;
          }

          .actions {
            flex-direction: row;
            padding-top: 0;
          }
        }
      }
    }
  }

  // Fix for skeleton-extras-list to ensure proper grid
  .skeleton-extras-list {
    display: grid !important;
    grid-template-columns: repeat(3, 1fr) !important;

    @media (max-width: 47.9375rem) {
      grid-template-columns: repeat(2, 1fr) !important;
    }

    @media (max-width: 37.5rem) {
      grid-template-columns: 1fr !important;
    }
  }
</style>
